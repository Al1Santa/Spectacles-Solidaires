{% extends 'base.html.twig' %}

{% block title %}Modifier une catégorie{% endblock %}

{% block body %}
    <div id="container"> 
        <div id="body_title">
            <h1>Modifier une catégorie</h1>
        </div>

        <div id="body_create">
            <a href="{{ path('app_category_index') }}">Retour à la liste</a>
        </div>

        {{ include('category/_form.html.twig', {'button_label': 'Mise à jour'}) }}
        <br>
        {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
