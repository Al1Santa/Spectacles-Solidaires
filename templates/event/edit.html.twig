{% extends 'base.html.twig' %}

{% block title %}Modifier l'évènement{% endblock %}

{% block body %}
    <div id="container"> 
        <div id="body_title">
            <h1>Modifier l'évènement</h1>
        </div>

        <div id="body_create">
            <a href="{{ path('app_event_index') }}">retour</a>
        </div>

        {{ include('event/_form.html.twig', {'button_label': 'mise à jour'}) }}
        <br>
        {{ include('event/_delete_form.html.twig') }}
    </div>
{% endblock %}
